<h1>User Index</h1>

<% @users.each do |user| %>
  <% if user.profile.nil? %>
    <p><%= link_to user.email, user_path(user) %></p>
  <% else %>
  <p><%= link_to user.full_name, user_path(user) %></p>
  <% end %>
<% end %>
