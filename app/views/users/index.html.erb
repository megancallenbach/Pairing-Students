<h1>User Index</h1>

<% @users.each do |user| %>
  <% if user.full_name.nil? %>
    <p><%= link_to user.email, user_path(user) %></p>
    <p><%= user.lonely_students %></p>
  <% else %>
  <p><%= link_to user.full_name, user_path(user) %></p>
  <% end %>
<% end %>
